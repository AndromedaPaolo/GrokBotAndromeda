# Password della dashboard /ops (obbligatoria in produzione).
OPS_PASSWORD=

# Cloudflare R2 — senza queste, gli upload restano solo in locale (dev).
# Su Vercel Hobby senza R2 i file NON restano dopo il deploy.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=fe-media
R2_ENDPOINT=
