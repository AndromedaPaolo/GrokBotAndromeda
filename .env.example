# Copy to .env and fill in to enable real Grok (xAI) responses.
# Without XAI_API_KEY the app runs in a self-contained local fallback mode.
XAI_API_KEY=
# Optional overrides:
# XAI_MODEL=grok-2-latest
# XAI_API_URL=https://api.x.ai/v1/chat/completions
# PORT=3000
# HOST=0.0.0.0
