import OpsClient from "./OpsClient";

export const metadata = {
  title: "Ops — Fantasy Empire",
  robots: { index: false, follow: false },
};

export default function OpsPage() {
  return <OpsClient />;
}
